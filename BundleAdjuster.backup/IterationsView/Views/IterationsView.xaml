<UserControl x:Class="BundleAdjuster.IterationsView.Views.IterationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BundleAdjuster.IterationsView.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <CheckBox IsChecked="{Binding TrackEveryIteration}" >
            <TextBlock Text="Track every iteration"></TextBlock>
        </CheckBox>
        <DataGrid x:Name="Datagrid"  x:FieldModifier="public" ItemsSource="{Binding Nrs}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HorizontalGridLinesBrush="Transparent" VerticalGridLinesBrush="Transparent"
                  BorderThickness="0"
                  Background="Transparent"
                  Grid.Row="1"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow" >

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nr" Width="30" Binding="{Binding}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
